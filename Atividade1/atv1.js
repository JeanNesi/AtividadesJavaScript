function botao(){ /*Quando o botão é pressionado chama está função*/
    var resultado = (10 * 4) - 8 + (24/8); /* Faço a equação e armazeno na variável*/
    document.querySelector(".resultado").innerHTML = resultado; /* Armazeno o resultado na classe do HTML */
}